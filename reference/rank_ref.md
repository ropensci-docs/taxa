# All known taxonomic ranks

A list of taxonomic ranks from all databases used combined into a single
vector to make it easier to maintain the relative order of ranks when
data from multiple databases are combined.

## Usage

``` r
rank_ref
```

## Format

An object of class `numeric` of length 52.

## Attribution

This list was adapted from a similar one in the `taxize` package.
