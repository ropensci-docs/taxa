# Returns the index of the first occurrence of each unique taxon in a taxonomy

Returns the index of the first occurrence of each unique taxon in a
taxonomy

## Usage

``` r
duplicated_index_taxonomy(x, proxy_func = NULL)
```
